To run SIMOC Remote

1. git clone https://github.com/Shantanu-Parmar/SimocRemote
2. pip install -r requirements.txt
   OR
2. pip install flask & pip install pandas
3. cd SimocRemote
4. python app.py

Ensure SIMOC Live is running in parallel (through run-tmux or multiple ssh windows)
